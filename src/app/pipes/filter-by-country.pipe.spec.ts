/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilterByCountryPipe } from './filter-by-country.pipe';

describe('FilterByCountryPipe', () => {
  it('create an instance', () => {
    const pipe: FilterByCountryPipe = new FilterByCountryPipe();
    expect(pipe).toBeTruthy();
  });
});
